# Database Configuration
# Use either DATABASE_URL (complete connection string) OR the individual DB_* variables below
# Copy this file to .env and fill in your actual credentials

# Option 1: Complete connection string (alternative)
# DATABASE_URL=mysql+pymysql://user:password@host:port/database_name

# Option 2: Individual database variables (recommended)
DB_HOST=your-database-host.mysql.database.azure.com
DB_PORT=3306
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# JWT Secret Key for token generation and validation
SECRET_KEY=your-secret-key-here

# MySQL SSL Certificate (optional, if required)
# MYSQL_SSL_CA=/path/to/cert.pem
